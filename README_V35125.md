# V35125 — Friendly Workflow UX

Frontend-focused correction after V35120 felt too confusing.

## Changes
- Rebuilt command centre into a simpler cockpit workflow.
- Added clear executive flow: Brief → Decide → Execute → Follow Up.
- Left navigation stays full but is grouped and easier to understand.
- Command input is secondary on Today, primary only on Run Command.
- Each section works as its own sub-page/tool on the belt.
- Added seeded test content across pages.
- Added smoother transitions without overloading the interface.
- Database page uses existing backend DB routes: /db-status, /db-items, /db-test-write.
- Backend preserved from V35120.

## Version Label
V35125 Friendly Workflow UX
